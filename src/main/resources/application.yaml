spring:
  application:
    name: forge-ai

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

info:
  app:
    name: Forge AI
    platform: OpenEIOS
